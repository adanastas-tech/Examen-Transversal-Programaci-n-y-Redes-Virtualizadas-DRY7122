---
- name: Configurar IPv6 en Loopback 33
  hosts: routers
  gather_facts: false
  
  tasks:
    - name: Crear Loopback 33 y asignar direcciones IPv6
      ios_config:
        lines:
          - ipv6 enable
          - ipv6 address 3001:ABCD:ABCD:1::1/128
          - ipv6 address FE80::1 link-local
        parents: interface Loopback33